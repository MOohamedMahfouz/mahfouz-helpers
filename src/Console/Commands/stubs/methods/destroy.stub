public function destroy({{modelName}} ${{variableName}})
{
    ${{variableName}}->deleteOrFail();

    return response()->json(
        $this->successResponse(
            '',
            []
        )
    );
}
